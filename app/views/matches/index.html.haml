.well.text-uppercase Listing matches

%table.table.table-striped
  %thead
    %tr
      %th Match no
      %th Match date
      %th Team
      %th Team1 Players
      %th Team2 Players
      %th Winning team
      %th Queen holder
      %th(colspan=2)

  %tbody
    - @matches.each do |match|
      %tr
        %td= match.match_no
        %td= match.match_date
        %td
          = match.team1.name 
          Vs
          = match.team2.name
        %td
          = match.player_1.try(:name)
          and
          = match.player_2.try(:name)
        %td
          = match.player_3.try(:name)
          and
          = match.player_4.try(:name)
        %td= match.winning_team.try(:name)
        %td= #match.try(:queen_holder).try(:name)
        %td= link_to 'Update score', edit_match_path(match)
        %td= link_to 'Destroy', match, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Match', new_match_path
